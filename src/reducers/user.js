export const initialState = {
    userName: '',
    userLastName: ''
}

export const userReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}